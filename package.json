{
  "name": "markdown-with-syntax-highlighting",
  "version": "0.1.0",
  "author": "YUKiYURi WEB",
  "scripts": {
    "start": "yarn dist",
    "readme": "php ./readme.php > details.json",
    "dist": "mkdir -p dists; rm dists/$npm_package_name.$npm_package_version.zip; wp dist-archive ./; mv ../$npm_package_name.$npm_package_version.zip ./dists/"
  },
  "devDependencies": {
    "html-minifier": "^4.0.0",
    "markdown": "^0.5.0"
  }
}
